// frontend/src/components/Auth/QuizPopup.jsx
import React from "react";

/**
 * Safe Quiz popup stub.
 * Keeps the component tree stable and avoids side-effects while debugging auth.
 * Replace with your original implementation once auth is stable.
 */
const QuizPopup = () => {
  return null;
};

export default QuizPopup;
